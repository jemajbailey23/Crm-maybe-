# Postgres connection string. For local dev, point at a local Postgres
# instance; in production, use your hosted provider's connection string
# (e.g. Neon, Supabase).
DATABASE_URL="postgresql://postgres:password@localhost:5432/crm_dev"

# Generate with: openssl rand -hex 32
AUTH_SECRET="replace-with-a-random-secret"
